<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ToolbarHeader" parent="TextAppearance.AppCompat.Headline"/>

    <style name="BetButton" parent="Widget.AppCompat.Button.Borderless"/>

    <style name="BetButton.Yes">
        <item name="colorAccent">@color/politikon_yes_green</item>
        <item name="android:textAppearance">@style/BetButtonText.YesButton</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="BetButton.No">
        <item name="colorAccent">@color/politikon_no_red</item>
        <item name="android:textAppearance">@style/BetButtonText.NoButton</item>
        <item name="android:gravity">right</item>
    </style>

</resources>
